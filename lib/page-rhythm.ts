/** Shared vertical rhythm for major section bands (homepage + long-scroll sections). */
export const PAGE_SECTION_PY = "py-12 md:py-16";

/** Oblica-like airy rhythm for Strategy A homepage shell (density dial 3). */
export const SHELL_SECTION_PY = "py-20 md:py-28 lg:py-32";

/** Content width for homepage shell (Oblica ~1200px). */
export const SHELL_CONTENT_MAX = "max-w-[1200px]";

/** Flex gap between major blocks on product detail sections. */
export const PAGE_STACK_GAP = "gap-12 md:gap-16";

/** Space between separator, intro, and content inside a section block. */
export const PAGE_BLOCK_SPACE_Y = "space-y-8 md:space-y-10";

/** Compact bands use the same section spacing for consistent rhythm. */
export const PAGE_COMPACT_BAND_PY = PAGE_SECTION_PY;

/** Shared spacing under section intro headings. */
export const SECTION_INTRO_BLOCK_MARGIN = "mb-8";

/** Standard row/column gap for homepage grids and stacked block rows (matches Våre produkter). */
export const HOME_PAGE_GRID_GAP = "gap-6";
